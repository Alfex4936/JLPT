---
target: the whole app UI
total_score: 28
max_score: 40
na_heuristics: 
p0_count: 0
p1_count: 4
target_identity: "file:/Users/ad03208797/Documents/0_github/jlpt/index.html"
target_fingerprint: "sha256:94596a44aeb87274cc50ae828aa6ce7945356d8b83825404f10047d3c6f33305"
target_path: /Users/ad03208797/Documents/0_github/jlpt/index.html
timestamp: 2026-09-17T10-46-02Z
slug: index-html
---
Method: dual-agent (A: design review, isolated · B: detector + browser evidence, isolated)

## Design Health Score — 28/40 (Good, bottom edge)

| # | Heuristic | Score | Key issue |
|---|---|---|---|
| 1 | Visibility of System Status | 2 | Making a sound — the app's most frequent action — produces no observable state change; `play()` rejections swallowed at app.js:1504 and :1539 |
| 2 | Match System / Real World | 3 | A 15s auto-advance timer bar sits 40px above a position counter, same visual kind, different quantity |
| 3 | User Control and Freedom | 3 | Exits are good; undo absent — both destructive resets have only native `confirm()`, a revealed mask cannot re-mask |
| 4 | Consistency and Standards | 2 | Three overlays hide three ways; six border-radius values against a brief declaring two; `.miss`/`.mask`/`.hanja` are boxes in an app whose brief says "카드 상자 없음" |
| 5 | Error Prevention | 3 | Good guards (maxlength, type=number max from deck length, confirm on resets, 연습 시작 disabled with a stated reason) |
| 6 | Recognition Rather Than Recall | 2 | `body.idle .chrome{opacity:0;pointer-events:none}` erases topbar and dock after 3000ms; mobile also strips labels to bare icons |
| 7 | Flexibility and Efficiency | 4 | 17 working shortcuts, hash deep-links, article-number jump, per-column kana selection, 틀린 글자만 다시 — expert paths everywhere, invisible to novices |
| 8 | Aesthetic and Minimalist Design | 3 | A column of 6 identical accent-outlined eye buttons plus 4 accent-outlined dock toggles; the accent stops distinguishing |
| 9 | Error Recovery | 3 | Drill recovery copy is exemplary, but a wrong answer wipes the field so you never see what you typed |
| 10 | Help and Documentation | 3 | One well-organised sheet plus live hints per settings section — but it is the only help, modal, and unreachable while chrome is faded |

Applicable max 40, nothing n/a.

## Design Specificity Verdict — grounded in this product

Assessment A judged this before seeing any detector output: the composition could not be dropped into an unrelated app. The word card's five stacked reading registers exist because a Korean learner of Japanese needs exactly those five. The `한자음 = 한국어` badge is a product insight rendered as UI — it gets the app's only accent colour, a filter tier, and a timing rule. The kana picker is the 五十音図 with 列 as the selection unit. Typography is argued from the script: Kosugi Maru is locked as the ruby face because at 13px Noto Sans JP merges strokes and ば/ぱ stop being distinguishable.

One surface is category-interchangeable: the settings drawer. 13 sections, 71 controls, label-left/switch-right rows, 2px range tracks — a competent iOS-settings clone that says nothing about JLPT.

Deterministic scan: 1 CLI finding (`clipped-overflow-container` on body), proven a false positive. Browser injection over a served origin: home, 단어, 한자, かな = 0 findings each; reading one-sentence = 23; reading full-article = 302.

## Priority Issues

- **[P1] Card autofit has no lower bound.** app.js:1261 floors `--scale` at 0.5 of the base, not the resulting pixel size. At 390x560 the word card renders its four example lines at 8.65px, meta at 6.61px, meaning numerals at 6.15px; 한자 mode reaches 7.88px. Nothing looks broken. Full-article reading takes `.art-jp` to 10.5px and furigana to 6.51px — which defeats the reason `--rt` was tuned in the first place. Fix: clamp the computed size (~13px body, ~9px ruby) and scroll the stage instead, reusing the `if (one) return` path at app.js:1251. `/impeccable adapt`
- **[P1] Ancestor opacity silently discounts the contrast tokens.** `.art-row.is-next` composites `--fg-2` from 7.43:1 to 2.13:1 at opacity .4 and 1.43:1 at .22; its border-top reads 1.04:1. `.hjp i.is-kun{opacity:.85}` composites to 3.82:1 dark and 3.66:1 light even after the token fix. Dimming is the right idea with the wrong instrument — it scales a value already at the floor. Fix: dim with a dedicated colour step, not opacity. `/impeccable colorize`
- **[P1] The closed settings panel stays in the tab order.** It closes by `transform:translateX(101%)` alone — `visibility:visible`, no `inert`, no `aria-hidden`. Measured 59 focusable controls, all 59 off-screen and all 59 tabbable. Fix: `inert` when `data-open="0"`. `/impeccable harden`
- **[P1] Audio has no feedback and swallows its own failures.** `try { a.play()['catch'](function () {}); } catch (e) {}` at app.js:1504 and :1539. Clip plays: you hear it, see nothing. Clip 404s: you hear a different voice, see nothing. Autoplay blocked or phone muted: you hear nothing, see nothing — and a rejected `play()` never fires `onerror`, so the TTS fallback never runs either. No class or dataset changes during playback anywhere. Fix: `data-speaking` on `<html>` from `onplaying`, cleared on `onended`/`onerror`, and a toast inside the existing catch. `/impeccable harden`
- **[P2] The end is inverted.** app.js:890 shows the full-screen 한 바퀴 완료 screen only when `missList()` is non-empty. A perfect round gets a 13px toast. The only ceremonial screen in the app is the failure report, with 40px type over accent-tinted boxes counting mistakes. Peak-end backwards. `/impeccable delight`
- **[P2] 가리기 모드 masks an answer the card prints twice.** Verified: meaning masked as "전문가", while `#cExO` reads "그의 솜씨는 전문가 수준이다." The recall mechanic does nothing. Fix: mask the example translation with it, reveal together in `reveal()`. `/impeccable clarify`

## Cognitive Load — 3 of 8 fail (moderate)

Chunking, minimal choices, and working memory fail. Decision points over 4 visible options: かな 글자 고르기 80, settings 71 across 13 sections, 일본어 글꼴 12, reading dock 10 at 1280, SRS word dock 11 across 4 wrapped rows. The home mode picker at 4 is the only surface respecting the limit.

Three co-visible working-memory loads: `정답 6 · 오답 0` (lifetime) versus `정답률 0%` (this round) ~700px apart and neither labelled; the timer bar above the position counter; `5자 중 2자를 틀렸습니다 · 정답률 71%` carrying two denominators in one sentence.

## Persona Red Flags

- **Casey (distracted mobile):** the first dock tap is always dead — chrome fades at 3000ms with `pointer-events:none`, and `touchstart` wakes it but the tap never reaches the button. Dock eats 25% of the viewport. 「17名死亡」 breaks 17 from its counter 名.
- **Sam (accessibility-dependent):** 59 off-screen tabbables in the closed panel; `.hjp i.is-kun` at 3.82:1.

## What's Working

- **The 五十音図 column picker.** Selection unit is 列, matching how the chart is learned — a 68-tap chore becomes one tap, romaji under every glyph means you need not know the answer to choose the drill, and `선택 71자` states round size before you commit.
- **Browser surfaces are themed.** focus ring, `::selection`, `scrollbar-color`, `caret-color`. Load-bearing rather than decorative: the kana input is a bare underline with no box or placeholder, so the caret is the affordance.
- **Expert paths.** 17 shortcuts, deep-links, the article jump — heuristic 7 is a genuine 4 and the app's strongest axis.

## False Positives and Retractions

- `clipped-overflow-container` on body: false positive. body sets no containing block (transform/filter/contain/perspective/will-change all none), so its `position:fixed` escape layers cannot be clipped; the only absolute child, `#volPop`, measures entirely inside the body box; `scrollWidth == clientWidth` at both 1863 and 390.
- `dark-glow` ×1 and `text-occlusion` ×67 in a second pass: the detector measuring its own overlays. It reported a `#ffba00` glow on body while body's computed box-shadow is `none`, and quoted its own label text. Removing `[class^="impeccable"]` nodes dropped the total 370 → 302.
- The 302 reading-mode findings are nesting-multiplied: the same glyph run is counted at `p.art-jp` (13), its `ruby` children (~120), and each `rt` (169). Roughly 14 rows, not 302 problems. The sizes themselves are real.
- `cramped-padding`: the rule's boundary test is colour-not-transparent rather than contrast, so it counts a border that composites to 1.04:1 as visible. 13 of 14 rows sit in the same state; 1 was reported.
- Assessment A retracted its own "Esc is inert, keyboard users are trapped" finding after re-reading app.js:1691, which calls `openKanaPick(false)`. Escape works; it costs two presses. P1 → P3, and heuristic 3 rose 2 → 3, moving the total from 27 to 28.
- Assessment A's "73 kana columns with aria-pressed null" does not hold. Measured: 68 column buttons, 0 with a null `aria-pressed`.
