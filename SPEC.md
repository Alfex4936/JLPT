# JLPT 낱말 셔플 (Quizlet 스타일) — 스펙

한국인 학습자용 JLPT N5~N1 단어 자동 슬라이드 웹앱. **완전 오프라인, `file://` 로 index.html 직접 열어 동작해야 한다** (fetch/XHR/모듈 import 금지 — 데이터는 `<script src>` 로 로드).

## 학습자 프로필 (설계 제약)
- 한국어 모어. 히라가나/가타카나는 읽을 수 있음. **한자는 전혀 모름.**
- 따라서 한자만 보여주면 정보 0. 항상 かな 읽기 + 한글 발음 표기를 같이 노출.
- 강점: 한국 한자음. `問題 = 문제`, `無理 = 무리` 처럼 한자음이 한국어 단어와 겹치는 경우가 매우 많음 → `hj` 필드가 최강의 암기 후크다. 눈에 잘 띄게 배치.

## 데이터 계약
`window.JLPT` 배열에 각 레벨 파일이 push 한다. index.html 은 5개 파일을 순서대로 script 태그로 로드하고, **없는 파일은 조용히 무시**(404 나도 앱 동작).

```html
<script>window.JLPT = window.JLPT || [];</script>
<script src="data/words-n5.js"></script>
<script src="data/words-n4.js"></script>
<script src="data/words-n3.js"></script>
<script src="data/words-n2.js"></script>
<script src="data/words-n1.js"></script>
```

각 데이터 파일: `window.JLPT.push(...[ {...}, {...} ])`

단어 객체:

| 키 | 뜻 | 예 | 없을 수 있음 |
|---|---|---|---|
| `i` | 고유 id (정수) | `12` | 아니오 |
| `lv` | 급수 5~1 | `5` | 아니오 |
| `w` | 표기 (한자 또는 かな) | `"毎朝"` | 아니오 |
| `k` | かな 읽기 | `"まいあさ"` | 아니오 |
| `h` | 한글 발음 표기 | `"마이아사"` | 아니오 |
| `hj` | 한국 한자음 | `"매조"` | **예** (かな 단어는 null) |
| `hjp` | 한자별 한자음 | `"毎매 朝조"` | **예** |
| `ko` | 한국어 뜻 배열, 최대 3 | `["매일 아침"]` | 아니오 |
| `p` | 품사(한국어) | `"명사"` | 아니오 |
| `e` | 예문 (일본어, 한자 포함) | `"毎朝コーヒーを飲みます。"` | 예 |
| `ek` | 예문의 かな 전용 읽기 | `"まいあさコーヒーをのみます。"` | 예 |
| `eh` | 예문 한글 발음, 어절 단위 공백 | `"마이아사 코히오 노미마스"` | 예 |
| `eo` | 예문 한국어 번역 | `"매일 아침 커피를 마십니다."` | 예 |
| `en` | 영어 뜻 (참고용, 기본 숨김) | `"every morning"` | 예 |
| `same` | 한자음이 한국어 뜻과 일치(`1`) — 한국인에게 사실상 공짜 어휘 | `1` | 예 (없으면 불일치) |

품사 값 집합: 명사 / 동사(타) / 동사(자) / い형용사 / な형용사 / 부사 / 조사 / 접속사 / 감탄사 / 접두사 / 접미사 / 대명사 / 수사 / 연체사 / 표현

## 카드 레이아웃 (정보 우선순위)
```
                                    N5 · 명사            ← 급수 배지 + 품사
        まいあさ                                          ← かな (읽을 수 있는 유일한 단서, 크게)
         毎朝                                             ← 표기, 가장 큼
      마이아사 · 한자음 매조                                 ← 한글 발음 + 한자음(하이라이트)
   ─────────────────────────────
        매일 아침                                          ← 뜻 (최대 3개, 번호)
   ─────────────────────────────
   毎朝コーヒーを飲みます。                                  ← 예문
   まいあさコーヒーをのみます。                               ← 예문 かな (한자 못 읽으므로 필수)
   매일 아침 커피를 마십니다.                                ← 예문 번역
```

## 기능 요구
- **자동 슬라이드**: 기본 15초, 설정에서 3~60초. 남은 시간 진행 바(부드럽게).
- **셔플**: 기본 ON. 시드 셔플로 세션 재현 가능. 순서대로 모드도 제공.
- **레벨 필터**: N5~N1 다중 선택. 선택 즉시 덱 재구성.
- **가리기 모드(요청 사항 아님, 권장)**: 뜻을 처음엔 가리고 N초 뒤/클릭 시 공개 → 회상 훈련. 토글 가능, 기본 OFF.
- **컨트롤**: 재생/일시정지, 이전/다음, 처음부터, 즐겨찾기(★).
- **키보드**: `Space` 재생/정지, `←/→` 이전/다음, `↑` 뜻 공개, `S` 즐겨찾기, `V` 발음, `?` 도움말.
- **발음(TTS)**: `speechSynthesis` 로 `ja-JP` 음성. 단어 클릭 또는 `V`. 자동 읽기 옵션.
- **즐겨찾기 덱**: ★ 한 단어만 모아 반복.
- **진행 상태 저장**: localStorage (설정, ★, 마지막 위치, 본 횟수).
- **모바일**: 좌우 스와이프로 이전/다음, 세로 한 화면에 카드 전체.
- **접근성**: 대비 충분, `prefers-reduced-motion` 존중, 포커스 링 유지.

## 폰트 (이미 다운로드 완료, `assets/fonts/`)
로컬 woff2 를 `@font-face` 로 로드하고, **반드시 시스템 폰트 폴백을 함께 지정**한다 (브라우저가 `file://` 에서 폰트를 막아도 앱이 멀쩡해야 함).

| 파일 | 용도 | family 이름 |
|---|---|---|
| `klee-one-japanese-400-normal.woff2`, `...-600-...` | **표기(한자) 대형 표시** — 교과서체 계열, 획이 또렷해 한자 학습에 최적 | `Klee One` |
| `noto-sans-jp-japanese-400-normal.woff2`, `...-700-...` | かな 읽기, 예문, 일본어 UI | `Noto Sans JP` |
| `noto-sans-jp-latin-400-normal.woff2` | 라틴/숫자 | `Noto Sans JP` |
| `noto-sans-kr-korean-400-normal.woff2`, `...-700-...` | 한국어 뜻·UI | `Noto Sans KR` |

폴백 스택 예: `"Klee One","Hiragino Mincho ProN","Noto Sans JP","Hiragino Kaku Gothic ProN","Apple SD Gothic Neo",system-ui,sans-serif`.
`font-display: swap`. 폰트 로딩 실패 감지 시(document.fonts.check) 조용히 시스템 폰트로 진행 — 경고 배너 띄우지 말 것.

## TTS (요구사항, 중요)
`speechSynthesis` 사용. **기본은 단어(`k` 또는 `w`)만 읽는다.**
- 켜기/끄기 토글 (기본 ON).
- **음량 슬라이더** 0~100% (`utterance.volume`), 속도 슬라이더 0.5~1.5 (`rate`).
- 음성 선택: `lang` 이 `ja` 로 시작하는 voice 목록 드롭다운 (macOS 는 보통 Kyoko/Otoya). ja 음성이 없으면 TTS 섹션에 "일본어 음성 없음" 표시하고 토글 비활성.
- 옵션: `예문도 읽기`(기본 OFF), `단어 두 번 읽기`(기본 OFF).
- 카드가 바뀌면 이전 발화 `cancel()` 후 새로 발화 (겹침 금지).
- 수동 재생: 단어 클릭 또는 `V`.
- `speechSynthesis.getVoices()` 는 비동기이므로 `voiceschanged` 이벤트 처리 필수.

## 앰비언트 모드 (모니터에 띄워두고 방치)
사용 패턴: 보조 모니터에서 전체화면으로 몇 시간 자동 재생.
- 전체화면 버튼(`F`) — Fullscreen API.
- 3초 무입력 시 컨트롤/설정 UI 페이드 아웃, 마우스 움직이면 복귀. 카드는 항상 보임.
- 전체화면에서는 타이포 스케일 업 (`clamp()` + vh 기반), 카드가 화면 중앙에 크게.
- 덱 끝에 도달하면 재셔플 후 무한 반복 (멈추지 않음).
- 화면 절전 방지: `navigator.wakeLock` 있으면 사용, 없으면 무시.
- 장시간 구동 안전: `setInterval` 누적 방지, 카드 전환 시 DOM 재사용, 메모리 누수 없게.

## 비기능
- 파일 구성: `index.html`, `assets/app.js`, `assets/style.css`, `assets/fonts/*`, `data/*.js`.
- 빌드 도구 없음. 외부 네트워크 요청 0 (폰트도 로컬). 완전 오프라인.
- 데이터 8천 개 로드 시에도 첫 페인트 빠르게, 렌더는 현재 카드만.
- `start.command`(더블클릭용): `python3 -m http.server` 로 로컬 서버 띄우고 브라우저 열기 — `file://` 제약을 확실히 피하는 경로도 제공.
